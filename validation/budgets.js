const Validator = require("validator");

module.exports = function validateBudgets(data) {
// key into user.budgetcategories.percentages => return arr 
// => sum values in arr === 100%, if not res(errors)
}